const playButton = document.querySelector(".play-button")
const pauseButton = document.querySelector(".pause-button")
const stopButton = document.querySelector(".stop-button")
const setButton = document.querySelector(".set-button")
const soundOnButton = document.querySelector(".sound-on")
const soundOffButton = document.querySelector(".sound-off")
const minutesPlaceholder = document.querySelector("#minutes")
const secondsPlaceholder = document.querySelector("#seconds")

export {
  playButton,
  pauseButton,
  stopButton,
  setButton,
  soundOffButton,
  soundOnButton,
  minutesPlaceholder,
  secondsPlaceholder,
}