let number = 1 // Isso tudo é uma expressão. a ; é facultativa no final.

console.log(typeof ++number) // Operador unário de incremento

console.log(number != 1 ? "alo" : "nada") // Operador ternário
